<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="io.github.gsantner.marowni"
          android:versionCode="8"
          android:versionName="1.3.2">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.Light">

        <activity
            android:name="io.github.gsantner.marowni.MainActivity"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name="io.github.gsantner.marowni.NoteActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <action android:name="android.intent.action.EDIT"/>
                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.DEFAULT"/>

                <data
                    android:mimeType="text/plain"
                    android:scheme="file"/>
                <data
                    android:mimeType="application/octet-stream"
                    android:scheme="file"/>
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data
                    android:mimeType="text/x-markdown"
                    android:scheme="file"/>
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>

                <data
                    android:host="*"
                    android:mimeType="*/*"
                    android:pathPattern="*.markdown"
                    android:scheme="file"/>
                <data
                    android:host="*"
                    android:mimeType="*/*"
                    android:pathPattern="*.md"
                    android:scheme="file"/>
            </intent-filter>
        </activity>

        <activity
            android:name="io.github.gsantner.marowni.PreviewActivity"
            android:label="@string/preview">
        </activity>

        <activity
            android:name="io.github.gsantner.marowni.settings.SettingsActivity"
            android:label="@string/action_settings"
            android:parentActivityName="io.github.gsantner.marowni.MainActivity"
            android:theme="@style/AppTheme.Light">
        </activity>

        <activity
            android:name="io.github.gsantner.marowni.PinActivity"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name="io.github.gsantner.marowni.AlphanumericPinActivity"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name="io.github.gsantner.marowni.PromptPinActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name="io.github.gsantner.marowni.widget.WidgetConfigure">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE"/>
            </intent-filter>
        </activity>

        <receiver android:name="io.github.gsantner.marowni.widget.WriteilyWidgetProvider">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/writeily_widget"/>
        </receiver>

        <service
            android:name="io.github.gsantner.marowni.widget.FilesWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS"/>
    </application>

</manifest>
